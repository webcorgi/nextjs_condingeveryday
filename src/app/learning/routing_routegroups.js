export default function RoutingRouteGroup() {
    // [1] (괄호) 쓰고 폴더 만들면 도메인 경로에 영향 주지 않음.
    // (marketing), (shop) 폴더 참고

    // [2] 하지만 ! (괄호) 안에 layout.js는 각각 지정 가능함.
    // 각각의 별도 레이아웃을 지정해줄 때 유용할 것으로 보인다.

    return (
        <></>
    )
}